'use client'

/**
 * Minimal performance monitor stub.
 * Reserved for future FPS-based adaptive quality; currently a no-op so the
 * Canvas tree stays stable. Rendered inside <Canvas> as a sibling of the scene.
 */
export function PerfMonitor() {
  return null
}
